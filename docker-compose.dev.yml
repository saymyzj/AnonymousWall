version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: anonymouswall_pg
    environment:
      POSTGRES_DB: anonymous_wall
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
